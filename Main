#pragma once

#include <iostream>
#include <sstream>
#include <string>



namespace Transfer
{
	struct TransferRequest
	{
		std::string ipAdress;
		std::string file;
	};

	TransferRequest getTransferRequest(std::istream* input);
	TransferRequest getTransferRequest(char** input, size_t length);

	bool isEmpty(Transfer::TransferRequest request);
}


